name: rawbt_clone
description: A thermal printer driver and utility app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  print_bluetooth_thermal: ^1.1.1
  esc_pos_utils_plus: ^2.0.3
  image: ^4.1.3
  permission_handler: ^11.3.1
  fluttertoast: ^8.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
